# EfficienCity - Resource Management System: Backend

The backend component currently consists of a minimal Docker Compose configuration that provisions a PostgreSQL database.
A full backend service was outside the scope of the MVP, so no API or server implementation is included.
