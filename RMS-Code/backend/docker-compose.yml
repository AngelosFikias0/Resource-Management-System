version: "3.8"
services:
  postgres:
    image: postgres:15
    container_name: rms_db
    environment:
      POSTGRES_USER: ADMIN
      POSTGRES_PASSWORD: PASSWORD
      POSTGRES_DB: rms_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
